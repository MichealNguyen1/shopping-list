# Copy thành .env.local cho dev local
# Khi dùng Vite proxy (vite.config.ts), VITE_API_URL không cần set
# Chỉ cần set khi deploy lên Vercel (trỏ đến Render URL)
VITE_API_URL=https://your-app.onrender.com/api
